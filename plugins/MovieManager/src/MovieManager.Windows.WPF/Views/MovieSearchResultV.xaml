<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:interfaces="clr-namespace:MovieManager.Plugins.Common.Interfaces;assembly=MovieManager.Plugins.Common">
    
  <DataTemplate x:Key="MM.DT.IMovieSearchResult" DataType="{x:Type interfaces:IMovieSearchResult}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <TextBlock Text="{Binding Name}" FontWeight="Bold"/>

      <StackPanel Grid.Row="1" Orientation="Horizontal">
        <StackPanel.Resources>
          <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="Margin" Value="2"/>
          </Style>
        </StackPanel.Resources>

        <TextBlock Text="Year:"/>
        <TextBlock Text="{Binding Year}"/>
        <TextBlock Text="Id:"/>
        <TextBlock Text="{Binding DetailId.Id}"/>
        <TextBlock Text="Type:"/>
        <TextBlock Text="{Binding Type}"/>
      </StackPanel>
    </Grid>
  </DataTemplate>
</ResourceDictionary>