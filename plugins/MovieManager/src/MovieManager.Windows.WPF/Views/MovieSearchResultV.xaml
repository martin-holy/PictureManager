<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:models="clr-namespace:MovieManager.Plugins.Common.Models;assembly=MovieManager.Plugins.Common">
    
  <DataTemplate x:Key="MM.DT.SearchResult" DataType="{x:Type models:SearchResult}">
    <Grid Margin="0,0,0,6" ToolTip="{Binding DetailId.Id}">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

      <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
      <TextBlock Grid.Row="1" Text="{Binding Desc}"/>
      <TextBlock Grid.Row="2" Text="{Binding TypeAndYear}"/>
    </Grid>
  </DataTemplate>
</ResourceDictionary>