<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:cc="clr-namespace:PictureManager.CustomControls"
                    xmlns:pm="clr-namespace:PictureManager"
                    xmlns:mhc="clr-namespace:MH.UI.WPF.Controls;assembly=MH.UI.WPF">
  
  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="../Converters.xaml" />
    <ResourceDictionary Source="../MixedStyles.xaml" />
    <ResourceDictionary Source="../NamedStyles/ButtonStyles.xaml" />
    <ResourceDictionary Source="../NamedStyles/BorderStyles.xaml" />
    <ResourceDictionary Source="../NamedStyles/TextBlockStyles.xaml" />
    <ResourceDictionary Source="../Icons.xaml" />
    <ResourceDictionary Source="/MH.UI.WPF;component/Resources/ControlStyles/IconStyle.xaml" />
  </ResourceDictionary.MergedDictionaries>
  
  <Style TargetType="{x:Type cc:SegmentControl}">
    <Setter Property="IsTabStop" Value="False" />
    <Setter Property="FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type cc:SegmentControl}">
          <Border BorderThickness="2" Margin="1" x:Name="PART_Border" Background="Transparent">
            <Border.Style>
              <Style TargetType="{x:Type Border}">
                <Setter Property="BorderBrush" Value="Black" />
                <Style.Triggers>
                  <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentColorBrush}" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </Border.Style>
    
            <Border.ToolTip>
              <ToolTip Padding="0" Visibility="{Binding DataContext, RelativeSource={RelativeSource Self}, Converter={StaticResource AllToVisibilityConverter}}">
                <DockPanel>
                  <Grid DockPanel.Dock="Top">
                    <!-- Thumbnail -->
                    <cc:MediaItemThumbnail DataContext="{Binding MediaItem}" />
                    
                    <!-- Segments Rects -->
                    <ItemsControl ItemsSource="{Binding MediaItemSegmentRects, RelativeSource={RelativeSource TemplatedParent}}">
                      <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                          <Canvas />
                        </ItemsPanelTemplate>
                      </ItemsControl.ItemsPanel>
                      <ItemsControl.ItemContainerStyle>
                        <Style TargetType="{x:Type ContentPresenter}">
                          <Setter Property="Canvas.Top" Value="{Binding Item1.Y}" />
                          <Setter Property="Canvas.Left" Value="{Binding Item1.X}" />
                        </Style>
                      </ItemsControl.ItemContainerStyle>
                      <ItemsControl.ItemTemplate>
                        <DataTemplate>
                          <Rectangle Width="{Binding Item1.Width}" Height="{Binding Item1.Height}" 
                                     StrokeThickness="1" Effect="{StaticResource Res_ShadowEffect}">
                            <Rectangle.Style>
                              <Style TargetType="{x:Type Rectangle}">
                                <Setter Property="Stroke" Value="White" />
                                <Style.Triggers>
                                  <DataTrigger Binding="{Binding Item2}" Value="True">
                                    <Setter Property="Stroke" Value="LimeGreen" />
                                  </DataTrigger>
                                </Style.Triggers>
                              </Style>
                            </Rectangle.Style>
                          </Rectangle>
                        </DataTemplate>
                      </ItemsControl.ItemTemplate>
                    </ItemsControl>
                  </Grid>
                  
                  <!-- Name and FileName -->
                  <StackPanel Orientation="Vertical" Margin="1">
                    <Border BorderThickness="1" BorderBrush="Black" Background="#88000000">
                      <TextBlock FontSize="18" Margin="5,2">
                        <TextBlock.Style>
                          <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Text" Value="{Binding Person.Name}" />
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding Person}" Value="{x:Null}">
                                <Setter Property="Text" Value="{Binding PersonId}" />
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </TextBlock.Style>
                      </TextBlock>
                    </Border>

                    <TextBlock Text="{Binding MediaItem.FileName}" TextWrapping="Wrap" MaxWidth="{Binding MediaItem.ThumbWidth}" />
                  </StackPanel>
                  
                </DockPanel>
              </ToolTip>
            </Border.ToolTip>
          
            <Grid>
              <Grid x:Name="PART_ImageGrid" >
                <Image Source="{Binding Picture}" Stretch="Fill"
                       Width="{Binding Segments.SegmentSize, Source={x:Static pm:App.Core}}"
                       Height="{Binding Segments.SegmentSize, Source={x:Static pm:App.Core}}" />
              
                <mhc:Icon Source="{StaticResource IconPeople}" Fill="Gray" Size="50">
                  <mhc:Icon.Style>
                    <Style TargetType="{x:Type mhc:Icon}" BasedOn="{StaticResource MH.UI.WPF.Controls.Icon}">
                      <Setter Property="Visibility" Value="Collapsed" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding Picture}" Value="{x:Null}">
                          <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </mhc:Icon.Style>
                </mhc:Icon>
              </Grid>
              
              <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal">
                  <StackPanel.Style>
                    <Style TargetType="{x:Type StackPanel}">
                      <Setter Property="Visibility" Value="Hidden" />
                      <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                          <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </StackPanel.Style>
                  
                  <!-- Same Person Button -->
                  <StackPanel Orientation="Horizontal">
                    <StackPanel.Style>
                      <Style TargetType="{x:Type StackPanel}">
                        <Setter Property="Visibility" Value="Hidden" />
                        <Style.Triggers>
                          <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                              <Condition Binding="{Binding IsSelected}" Value="True" />
                              <Condition Binding="{Binding Segments.SelectedCount, Source={x:Static pm:App.Core}, Converter={StaticResource AllToBoolConverter}, ConverterParameter=1}" Value="False" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible" />
                          </MultiDataTrigger>
                        </Style.Triggers>
                      </Style>
                    </StackPanel.Style>
                    
                    <Border Background="#1181B1" Style="{StaticResource SBorderGroup}" VerticalAlignment="Center" Margin="0,0,2,0">
                      <TextBlock Text="{Binding Segments.SelectedCount, Source={x:Static pm:App.Core}}" Style="{StaticResource STextBlock}" />
                    </Border>

                    <mhc:IconButton x:Name="PART_BtnSamePerson"
                                    Source="{StaticResource IconEquals}"
                                    Size="14"
                                    ToolTip="Set selected as same person" />
                  </StackPanel>

                  <mhc:IconButton x:Name="PART_BtnDetail"
                                  Source="{StaticResource IconInformation}"
                                  Size="14" />
                  
                </StackPanel>

                <mhc:Icon Source="{StaticResource IconCheckmark}"
                          Fill="LimeGreen"
                          Size="16"
                          Margin="0,0,2,0"
                          ShowShadow="True"
                          IsIconVisible="{TemplateBinding IsCheckmarkVisible}" />
              </StackPanel>
            </Grid>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  
</ResourceDictionary>