<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="../Brushes/Brushes.xaml" />
  </ResourceDictionary.MergedDictionaries>

  <Style x:Key="MH.Styles.Controls.GridSplitter.Preview">
    <Setter Property="Control.Template">
      <Setter.Value>
        <ControlTemplate>
          <Rectangle Fill="{StaticResource ColorBrushForeground}" Opacity="0.5" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="MH.Styles.Controls.GridSplitter" TargetType="{x:Type GridSplitter}">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="HorizontalAlignment" Value="Stretch" />
    <Setter Property="PreviewStyle" Value="{StaticResource MH.Styles.Controls.GridSplitter.Preview}" />
    <Setter Property="ShowsPreview" Value="True" />
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="Width" Value="3" />
  </Style>

  <Style TargetType="{x:Type GridSplitter}" BasedOn="{StaticResource MH.Styles.Controls.GridSplitter}" />
</ResourceDictionary>
