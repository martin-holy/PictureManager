<UserControl x:Class="PictureManager.UserControls.SegmentsDrawerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:pm="clr-namespace:PictureManager"
             xmlns:views="clr-namespace:PictureManager.Views">
  
    <ItemsControl x:Name="SegmentsGrid" Margin="2,0,0,0"
                  ItemsSource="{Binding SegmentsM.SegmentsDrawer, Source={x:Static pm:App.Core}}">
      <ItemsControl.Template>
        <ControlTemplate>
          <ScrollViewer>
            <ItemsPresenter />
          </ScrollViewer>
        </ControlTemplate>
      </ItemsControl.Template>
      
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <WrapPanel />
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>
      
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <views:SegmentV Segment="{Binding}"
                          SelectedEventHandler="OnSegmentSelected" />
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>