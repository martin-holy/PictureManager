<UserControl x:Class="PictureManager.UserControls.PeopleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cc="clr-namespace:PictureManager.CustomControls">
  <Grid>
    <cc:VirtualizingWrapPanel x:Name="PeopleGrid" ScrollViewerSpeedFactor="1"
      ShowGroupItemsCount="True" GroupItemsCountIcon="ImageMultiple" SizeChanged="ControlSizeChanged">
      
      <cc:VirtualizingWrapPanel.ItemDataTemplate>
        <DataTemplate>
          <cc:SegmentControl DataContext="{Binding Segment}" Selected="OnSegmentSelected">
            <cc:SegmentControl.Style>
              <Style TargetType="{x:Type cc:SegmentControl}">
                <Setter Property="IsCheckmarkVisible" Value="False" />
                <Style.Triggers>
                  <DataTrigger Binding="{Binding Person.Segments, Converter={StaticResource AllToBoolConverter}}" Value="True">
                    <Setter Property="IsCheckmarkVisible" Value="True" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </cc:SegmentControl.Style>
          </cc:SegmentControl>
        </DataTemplate>
      </cc:VirtualizingWrapPanel.ItemDataTemplate>
    </cc:VirtualizingWrapPanel>
  </Grid>
</UserControl>
