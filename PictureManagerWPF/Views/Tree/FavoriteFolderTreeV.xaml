<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tree="clr-namespace:PictureManager.ViewModels.Tree"
                    xmlns:mhc="clr-namespace:MH.UI.WPF.Controls;assembly=MH.UI.WPF">

  <HierarchicalDataTemplate DataType="{x:Type tree:FavoriteFolderTreeVM}">
    <Grid Background="Transparent"
          ToolTip="{Binding Model.Folder.FullPath}">
      <StackPanel Orientation="Horizontal"
                  Margin="{Binding Converter={StaticResource CatTreeViewMarginConverter}}">
        <mhc:Icon Source="{StaticResource IconFolder}" Fill="{StaticResource ColorBrushFolder}" />
        <TextBlock Style="{StaticResource TreeItemTitle}" Text="{Binding Model.Title}" />
      </StackPanel>
    </Grid>
    
  </HierarchicalDataTemplate>

</ResourceDictionary>