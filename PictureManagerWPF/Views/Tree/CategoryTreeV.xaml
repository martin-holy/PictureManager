<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mhc="clr-namespace:MH.UI.WPF.Controls;assembly=MH.UI.WPF"
                    xmlns:ctvm="clr-namespace:PictureManager.Domain.CatTreeViewModels;assembly=PictureManager.Domain">

  <HierarchicalDataTemplate DataType="{x:Type ctvm:CatTreeViewCategory}" ItemsSource="{Binding Items}">
    <Border Height="40">
      <StackPanel Orientation="Horizontal"
                  Margin="{Binding Converter={StaticResource CatTreeViewMarginConverter}}">
        <mhc:Icon Source="{Binding Category, Converter={StaticResource CategoryToPathGeometryConverter}}"
                  Fill="{Binding Category, Converter={StaticResource CategoryToBrushConverter}}"
                  Size="24" />
        <TextBlock Style="{StaticResource TreeItemTitle}" Text="{Binding Name}" FontSize="18" />
      </StackPanel>
    </Border>
  </HierarchicalDataTemplate>

</ResourceDictionary>