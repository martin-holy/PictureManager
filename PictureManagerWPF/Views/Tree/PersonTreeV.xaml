<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:tree="clr-namespace:PictureManager.ViewModels.Tree"
                    xmlns:mhc="clr-namespace:MH.UI.WPF.Controls;assembly=MH.UI.WPF"
                    xmlns:pm="clr-namespace:PictureManager"
                    xmlns:b="http://schemas.microsoft.com/xaml/behaviors">
  
  <HierarchicalDataTemplate DataType="{x:Type tree:PersonTreeVM}">
    <Grid Background="{Binding DisplayFilter, Converter={StaticResource DisplayFilterToBrushConverter}}">
      <!-- ToolTip -->
      <Grid.Style>
        <Style TargetType="{x:Type Grid}">
          <Style.Triggers>
            <DataTrigger Binding="{Binding BaseVM.Model.Segment, Converter={StaticResource AllToBoolConverter}}" Value="True">
              <Setter Property="ToolTip">
                <Setter.Value>
                  <ToolTip BorderThickness="0" Padding="0">
                    <Border BorderThickness="1" BorderBrush="Black">
                      <Image Source="{Binding BaseVM.Model.Segment.Picture}"
                             Width="{Binding Segments.SegmentSize, Source={x:Static pm:App.Core}}" />
                    </Border>
                  </ToolTip>
                </Setter.Value>
              </Setter>
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </Grid.Style>

      <!-- Load Person Segment -->
      <b:Interaction.Triggers>
        <b:EventTrigger EventName="MouseEnter">
          <b:InvokeCommandAction Command="{Binding SegmentsBaseVM.SetSegmentPictureCommand, Source={x:Static pm:App.Ui}}"
                                 CommandParameter="{Binding BaseVM.Model.Segment}"/>
        </b:EventTrigger>
      </b:Interaction.Triggers>

      <StackPanel Orientation="Horizontal"
                  Margin="{Binding Converter={StaticResource CatTreeViewMarginConverter}}">
        <mhc:Icon Source="{StaticResource IconPeople}" Fill="{StaticResource ColorBrushPeople}" />
        <TextBlock Style="{StaticResource TreeItemTitle}" Text="{Binding BaseVM.Model.Name}" />
        <TextBlock Style="{StaticResource TreeItemKeywordsPicCount}" />
      </StackPanel>
    </Grid>
  </HierarchicalDataTemplate>

</ResourceDictionary>