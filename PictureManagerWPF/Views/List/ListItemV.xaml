<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mhuInt="clr-namespace:MH.Utils.Interfaces;assembly=MH.Utils"
  xmlns:rd="clr-namespace:PictureManager.ResourceDictionaries">

  <DataTemplate x:Key="PM.Views.List.ListItemV" DataType="{x:Type mhuInt:IListItem}">
    <DockPanel>
      <Path
        DockPanel.Dock="Left"
        Style="{StaticResource MH.Styles.Icon}"
        Data="{Binding Icon, Converter={StaticResource ResourceConverter}}"
        Fill="{Binding Icon, Converter={StaticResource ResourceConverter},
               ConverterParameter={x:Static rd:Dictionaries.IconNameToBrush}}"/>
      
      <TextBlock
        Style="{StaticResource PM.Styles.TextBlockBase}"
        Text="{Binding Name}"/>
    </DockPanel>
  </DataTemplate>
</ResourceDictionary>