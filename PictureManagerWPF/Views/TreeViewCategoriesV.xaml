<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:vm="clr-namespace:PictureManager.ViewModels"
  xmlns:pm="clr-namespace:PictureManager"
  xmlns:mhc="clr-namespace:MH.UI.WPF.Controls;assembly=MH.UI.WPF"
  xmlns:rd="clr-namespace:PictureManager.ResourceDictionaries"
  xmlns:cc="clr-namespace:PictureManager.CustomControls"
  xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
  xmlns:mhbc="clr-namespace:MH.UI.WPF.BaseClasses;assembly=MH.UI.WPF">

  <DataTemplate DataType="{x:Type vm:TreeViewCategoriesVM}">
    <DataTemplate.Resources>
      <ResourceDictionary>
        <ResourceDictionary.MergedDictionaries>
          <ResourceDictionary Source="../ResourceDictionaries/CustomControlStyles/CatTreeViewStyle.xaml" />
          <ResourceDictionary Source="TreeViewSearchV.xaml" />
          <ResourceDictionary Source="Tree/CategoryGroupTreeV.xaml" />
          <ResourceDictionary Source="Tree/CategoryTreeV.xaml" />
          <ResourceDictionary Source="Tree/DriveTreeV.xaml" />
          <ResourceDictionary Source="Tree/FavoriteFolderTreeV.xaml" />
          <ResourceDictionary Source="Tree/FolderKeywordTreeV.xaml" />
          <ResourceDictionary Source="Tree/FolderTreeV.xaml" />
          <ResourceDictionary Source="Tree/GeoNameTreeV.xaml" />
          <ResourceDictionary Source="Tree/KewordTreeV.xaml" />
          <ResourceDictionary Source="Tree/MediaItemSizeTreeV.xaml" />
          <ResourceDictionary Source="Tree/PersonTreeV.xaml" />
          <ResourceDictionary Source="Tree/RatingTreeV.xaml" />
          <ResourceDictionary Source="Tree/TreeContextMenus.xaml" />
          <ResourceDictionary Source="Tree/ViewerTreeV.xaml" />
        </ResourceDictionary.MergedDictionaries>
      </ResourceDictionary>
    </DataTemplate.Resources>
    
    <Grid Background="{StaticResource ColorBrushBackground}">

      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <Border
        Grid.Row="0"
        BorderThickness="0,0,0,4"
        BorderBrush="{StaticResource AccentColorBrush4}">
        
        <DockPanel Height="34">

          <!-- TreeView Categories Navigation Bar -->
          <ItemsControl
            ItemsSource="{Binding Items}"
            DockPanel.Dock="Left">

            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <mhc:IconButton
                  Size="22"
                  ShowShadow="False"
                  ToolTip="{Binding Name}"
                  Command="{Binding TreeViewCategoriesVM.ScrollToCommand,
                            Source={x:Static pm:App.Ui}}"
                  CommandParameter="{Binding}"
                  Source="{Binding Category,
                           Converter={StaticResource ResourceConverter},
                           ConverterParameter={x:Static rd:Dictionaries.CategoryToIconName}}"
                  Fill="{Binding Category,
                         Converter={StaticResource ResourceConverter},
                         ConverterParameter={x:Static rd:Dictionaries.CategoryToBrush},
                         FallbackValue={StaticResource ColorBrushWhite}}" />
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>

          <StackPanel
            DockPanel.Dock="Right"
            Orientation="Horizontal"
            HorizontalAlignment="Right">

            <!-- Search -->
            <mhc:IconButton
              Size="22"
              Source="{StaticResource IconMagnify}"
              ToolTip="Search"
              Command="{Binding ShowSearchCommand}"
              ShowShadow="False" />

            <!-- Button to pin slide panel -->
            <mhc:IconToggleButton
              Command="{Binding ToggleIsPinnedCommand}"
              Source="{StaticResource IconPin}"
              Size="14"
              IsThreeState="False"
              ShowShadow="False"
              IsChecked="{Binding IsPinned}"
              ToolTip="Pin panel" />
          </StackPanel>
        </DockPanel>
      </Border>

      <!-- CatTreeView Categories -->
      <ContentPresenter
        Grid.Row="1"
        Content="{Binding TvCategories}">
        
        <ContentPresenter.Resources>
          <Style
            TargetType="{x:Type cc:CatTreeView}"
            BasedOn="{StaticResource CatTreeView}">

            <Setter
              Property="FocusManager.IsFocusScope"
              Value="True" />
            <Setter
              Property="ItemsSource"
              Value="{Binding Items}" />
            <Setter
              Property="VirtualizingPanel.IsVirtualizing"
              Value="True" />
            <Setter
              Property="VirtualizingPanel.VirtualizationMode"
              Value="Recycling" />
            <Setter
              Property="ScrollViewer.HorizontalScrollBarVisibility"
              Value="Auto" />
    
            <Setter
              Property="ItemContainerStyle">
              <Setter.Value>
                <Style
                  TargetType="{x:Type TreeViewItem}"
                  BasedOn="{StaticResource CatTreeViewItem}">

                  <Setter
                    Property="VerticalContentAlignment"
                    Value="Stretch" />
                  <Setter
                    Property="HorizontalContentAlignment"
                    Value="Stretch" />
                  <Setter
                    Property="VirtualizingPanel.IsVirtualizing"
                    Value="True" />
                  <Setter
                    Property="VirtualizingPanel.VirtualizationMode"
                    Value="Recycling" />

                  <Style.Triggers>
                    <DataTrigger
                      Binding="{Binding Path=(mhbc:CatTreeViewItem.IsHidden)}"
                      Value="True">

                      <Setter
                        Property="Visibility"
                        Value="Collapsed" />
                    </DataTrigger>
                  </Style.Triggers>
                </Style>
              </Setter.Value>
            </Setter>

            <!-- BlurEffect when Search is visible -->
            <Style.Triggers>
              <DataTrigger
                Binding="{Binding TreeViewSearchVM.IsVisible}"
                Value="True">

                <Setter
                  Property="Effect">
                  <Setter.Value>
                    <BlurEffect
                      Radius="4"
                      KernelType="Gaussian" />
                  </Setter.Value>
                </Setter>
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </ContentPresenter.Resources>

        <b:Interaction.Triggers>
          <b:EventTrigger EventName="MouseLeftButtonUp">
            <b:InvokeCommandAction
              Command="{Binding SelectCommand}"
              PassEventArgsToCommand="True" />
          </b:EventTrigger>
        </b:Interaction.Triggers>
      </ContentPresenter>

      <!-- Search -->
      <ContentPresenter
        Grid.Row="1"
        Content="{Binding TreeViewSearchVM}" />

    </Grid>
  </DataTemplate>
    
</ResourceDictionary>