<ResourceDictionary
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:m="clr-namespace:PictureManager.Domain.Models;assembly=PictureManager.Domain"
  xmlns:pmConv="clr-namespace:PictureManager.Converters">

  <DataTemplate DataType="{x:Type m:MediaItemM}">
    <Border
      Width="{Binding ThumbWidth}"
      Height="{Binding ThumbHeight}"
      BorderThickness="2"
      BorderBrush="Black"
      ClipToBounds="True">

      <Grid>
        <Image Stretch="Fill">
          <Image.Source>
            <MultiBinding Converter="{pmConv:MediaItemThumbSourceConverter}">
              <Binding Path="FilePathCache" IsAsync="True"/>
              <Binding/>
            </MultiBinding>
          </Image.Source>
        </Image>

        <ItemsControl
          ItemsSource="{Binding InfoBoxThumb}"
          Style="{StaticResource InfoBoxThumbStyle}"/>
      </Grid>
    </Border>
  </DataTemplate>
</ResourceDictionary>